#include <iostream>
#include <sstream>
#include "segment_tree.h"

#ifdef _DEBUG
#endif

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	segment_tree_testing();

	std::cin.get();
	return 0;
}